version: "3.9"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            -   ./screenshots:/app/screenshots
        command: python3 main.py